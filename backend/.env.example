# ──────────────────────────────────────────────────────────────
# Vulnerable E-Commerce Platform — Environment Variables
# ──────────────────────────────────────────────────────────────
# Copy this file to .env and fill in values.
#   cp .env.example .env
# ──────────────────────────────────────────────────────────────

# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL via Prisma)
DATABASE_URL=postgresql://user:password@localhost:5432/vuln_ecommerce?schema=public

# JWT — VULNERABLE: intentionally weak secret
JWT_SECRET=secret123
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Redis (optional – for cart/session caching)
REDIS_URL=redis://localhost:6379

# OAuth – Google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/v1/auth/google/callback

# File Uploads
UPLOAD_DIR=public/uploads
MAX_FILE_SIZE=10485760

# Email (mock SMTP)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=

# Frontend URL (for CORS, emails, etc.)
FRONTEND_URL=http://localhost:5173
